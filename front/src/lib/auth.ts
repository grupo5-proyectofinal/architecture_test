export type User = {
    id: string,
    email: string,
    password: string,
}

/* import { get } from "svelte/store";
import { usersStore } from "./path/to/usersStore"; // Adjust the import path as necessary

export function createSessionByEmail(email: string, password: string){
    const currentUser = get(usersStore);
} */